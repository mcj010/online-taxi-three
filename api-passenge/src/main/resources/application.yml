server:
  port: 8080
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:7900/eureka/
spring:
  application:
    name: api-passenger
logging:
  level:
    root: info
devtools:
  livereload:
    enabled: true #是否支持livereload
  restart:
    enabled: true #是否支持热部署